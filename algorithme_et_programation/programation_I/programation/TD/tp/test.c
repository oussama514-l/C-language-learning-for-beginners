#include<stdio.h>
#include<string.h>

void siasire(int T[],int n){
	for(int i=0;i<n;i++){}
}
int main(){
	int T[5];
	int U[2+1];
	char C[7];
	char D[1+1];
	
	//remplire les tableaux
/*	
	saisire(T,5);
	saisire(U,5);
	saisire(C,7);
	saisire(D,2);
	printf("C %d\n",strlen(C));
	printf("D %d\n",strlen(D));

	affich(T);	
	affich(T);	
	affich(T);	
	affich(T);	
*/	

}
